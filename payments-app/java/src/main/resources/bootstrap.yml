server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: payments-app
  profiles:
    active: composite
  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: "file://${user.home}/config"
        bootstrap: true
        prefix: config